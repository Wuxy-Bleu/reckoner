spring:
  cloud:
    openfeign:
      client:
        config:
          postgresClient:
            url: "http://127.0.0.1:8080/v1/accounts"
          postgresReckonerClient:
            url: "http://127.0.0.1:8080/v1/reckoner"
    config:
      name: data-postgre
  config:
    import: configserver:http://127.0.0.1:9222
  devtools:
    restart:
      enabled: false